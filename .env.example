# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=quality_compass

# Backend Configuration
DEBUG=false
CORS_ORIGINS=http://localhost:4200,http://localhost

# Port Configuration
DB_PORT=5433
BACKEND_PORT=8000

# PostgreSQL Tuning (Optional)
# PG_SHM_SIZE=512mb
# PG_SHARED_BUFFERS=256MB
# PG_WORK_MEM=64MB
# PG_MAINTENANCE_WORK_MEM=128MB
# PG_EFFECTIVE_CACHE_SIZE=1GB
# PG_MAX_PARALLEL_WORKERS=2

# Note: RUNS_ROOT is no longer needed!
# Artifacts are read from the shared 'needle-artifacts' Docker volume.
# Run `docker volume create needle-artifacts` once before starting.
